<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BackupAutonomi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation</item>

    </style>

    <style name="Theme.BackupAutonomi" parent="Base.Theme.BackupAutonomi" />
    <style name="CustomActivityAnimation">
        <item name="android:activityOpenEnterAnimation">@anim/in_anim</item>
        <item name="android:activityOpenExitAnimation">@anim/out_anim</item>
    </style>
</resources>